# Imperial Motorcycles Web Platform

A React + Vite marketing site for a motorcycle repair & service workshop (services, gallery, news, and contact pages).

## Run Locally

**Prerequisites:** Node.js

1. Install dependencies:
   `npm install`
2. Run the dev server:
   `npm run dev`
3. Build for production:
   `npm run build`
